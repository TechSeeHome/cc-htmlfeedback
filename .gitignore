# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# cc-htmlfeedback runtime queue (connected mode)
.cc-htmlfeedback/

# Local agent/session scratch (not for a public repo)
.remember/
.claude/

# Editor / IDE
.idea/
.vscode/
*.swp

# Internal design docs / planning (kept locally, not published)
docs/superpowers/
docs/design/
.playwright-mcp

# Defensive: OAuth secrets live in agent state dirs outside any repo;
# these guard against a stray in-repo copy ever being staged.
**/client_secret.json
**/token.json

# c8 coverage output
coverage/
